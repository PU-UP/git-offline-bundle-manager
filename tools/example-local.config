# Example local configuration file
# Copy this file to local.config and modify the values

[main]
# Path to the local repository
repo_path=~/projects/slam-core

# Directory where bundle files are stored (USB drive, network share, etc.)
bundle_source=/media/usb

# Output directory for exported bundles (optional, defaults to ./bundles)
output_dir=./bundles

# 主分支名（可选，优先级最高）
# base_branch=release_2.3.7

# Default feature branch name (optional)
feature_branch=dev/awesome-feature

# Comma-separated list of submodules (optional)
# If not specified, will auto-detect from .gitmodules file
# modules=module_a,module_b

# Submodule branch mapping (optional)
# Format: module_name:branch_name,module_name2:branch_name2
# If not specified, will use the same branch name as main repository
# submodule_branches=module_a:dev/feature-a,module_b:dev/feature-b 